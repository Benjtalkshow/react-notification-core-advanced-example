import type { Notification } from "../types";
type GroupingFunction<T> = (notifications: Notification[]) => Record<string, T[]>;
/**
 * Hook for grouping notifications
 */
export declare const useNotificationGroups: <T extends Notification = Notification>(groupingFn?: GroupingFunction<T>) => {
    groupedNotifications: Record<string, T[]>;
    groups: string[];
    getNotificationsForGroup: (groupKey: string) => T[];
    groupedByType: Record<string, Notification[]>;
};
export {};
//# sourceMappingURL=useNotificationGroups.d.ts.map