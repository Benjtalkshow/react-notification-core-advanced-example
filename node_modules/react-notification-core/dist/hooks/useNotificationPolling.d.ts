import type { UseNotificationPollingOptions } from "../types";
/**
 * Hook for polling notifications at regular intervals
 */
export declare const useNotificationPolling: ({ enabled, interval, onError, retryCount, retryDelay, }?: UseNotificationPollingOptions) => {
    refresh: () => Promise<void>;
    stopPolling: () => void;
    startPolling: () => void;
    isPolling: () => boolean;
};
//# sourceMappingURL=useNotificationPolling.d.ts.map