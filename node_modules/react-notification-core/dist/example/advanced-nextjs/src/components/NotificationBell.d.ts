import type { Notification } from "react-notification-core";
interface NotificationBellProps {
    unreadCount: number;
    onClick: () => void;
    isOpen: boolean;
    onClose: () => void;
    notifications: Notification[];
    markAsRead: (id: string) => Promise<void>;
    markAllAsRead: () => Promise<void>;
    deleteNotification: (id: string) => Promise<void>;
    isLoading: boolean;
    error: string | null;
}
export default function NotificationBell({ unreadCount, onClick, isOpen, onClose, notifications, markAsRead, markAllAsRead, deleteNotification, isLoading, error, }: NotificationBellProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=NotificationBell.d.ts.map